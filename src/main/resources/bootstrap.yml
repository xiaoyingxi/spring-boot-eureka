spring:
  application:
    name: eureka
  profiles.active: svt
  cloud.config.failFast: false

#server:
  #port: 8761
#management:
  #server:
  #  port: 8762
  #  security.enabled: false
  #  health:
  #    diskspace.enabled: false
  #  endpoint:
  #    health.enabled: true
  #endpoints:
  #  web:
  #    base-path: /manage
  #    path-mapping.health: status


---
spring:
  profiles: dev
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}

---
spring:
  profiles: svt
  cloud:
    config:
      uri: http://yuuyoo-config:8888
---
spring:
  profiles: pro
  cloud:
    config:
      uri: http://yuuyoo-config:8888
